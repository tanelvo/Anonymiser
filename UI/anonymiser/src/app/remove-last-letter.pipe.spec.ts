import { RemoveLastLetterPipe } from './remove-last-letter.pipe';

describe('RemoveLastLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLastLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
